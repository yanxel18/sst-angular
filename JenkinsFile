pipeline{
  agent any
    stages {
      stage('Install Dependency') {
        steps{
          bat 'npm install'
        }
      }
    stage('SonarQube analysis') {
      steps {
        bat 'npm run sonar'
      }
    }
  }
}
